<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/view_db_schedule"
        android:icon="@drawable/ic_action_calendar_day"
        android:orderInCategory="20"
        android:title="@string/menu_schedule_backups"
        app:showAsAction="ifRoom|withText" />
    <item
        android:id="@+id/cleanup_bu"
        android:icon="@drawable/ic_action_clear"
        android:orderInCategory="10"
        android:title="@string/pref_clear_title"
        app:showAsAction="ifRoom|withText" />

</menu>